# =====================================================================================
# GloboTicket Docker Environment Configuration Template
# =====================================================================================
# This file contains template environment variables for the Docker infrastructure.
# 
# USAGE:
#   1. Copy this file to .env: cp .env.example .env
#   2. Update the values in .env with your actual passwords
#   3. Never commit .env to version control (it's in .gitignore)
#
# SECURITY NOTE:
#   The .env file should contain actual passwords and is excluded from git.
#   This .env.example file is committed to provide a template for other developers.
# =====================================================================================

# -----------------------------------------------------------------------------
# SQL Server System Administrator (SA) Account
# -----------------------------------------------------------------------------
# Used for administrative tasks and initial setup
# Must meet SQL Server password requirements:
#   - Minimum 8 characters
#   - Contains uppercase letters (A-Z)
#   - Contains lowercase letters (a-z)
#   - Contains digits (0-9)
#   - Contains special characters (!@#$%^&*()_+-=[]{}|;:,.<>?)
SA_PASSWORD=YourStrong!Passw0rd

# -----------------------------------------------------------------------------
# Migration User Credentials
# -----------------------------------------------------------------------------
# User with elevated privileges for schema changes (DDL operations)
# Used ONLY for running EF Core migrations
# Should be used during deployment/migration windows only
MIGRATION_USER=migration_user
MIGRATION_PASSWORD=Migration@Pass123

# -----------------------------------------------------------------------------
# Application User Credentials
# -----------------------------------------------------------------------------
# User with restricted privileges for normal runtime operations (DML only)
# Used by the API during normal operation
# Cannot modify schema - follows principle of least privilege
APP_USER=app_user
APP_PASSWORD=YourStrong@Passw0rd

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Name of the GloboTicket database
DATABASE_NAME=GloboTicket

# SQL Server port (default: 1433)
SQL_SERVER_PORT=1433

# SQL Server edition (Developer, Express, Standard, Enterprise)
# Developer edition is free and has full features
MSSQL_PID=Developer

# -----------------------------------------------------------------------------
# Connection String Templates
# -----------------------------------------------------------------------------
# These are for reference - actual connection strings should be built from above variables
#
# For Migrations (using migration_user):
#   Server=localhost,${SQL_SERVER_PORT};Database=${DATABASE_NAME};User Id=${MIGRATION_USER};Password=${MIGRATION_PASSWORD};TrustServerCertificate=true;Encrypt=true
#
# For Application Runtime (using app_user):
#   Server=localhost,${SQL_SERVER_PORT};Database=${DATABASE_NAME};User Id=${APP_USER};Password=${APP_PASSWORD};TrustServerCertificate=true;Encrypt=true
#
# For Administrative Tasks (using sa):
#   Server=localhost,${SQL_SERVER_PORT};Database=${DATABASE_NAME};User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=true;Encrypt=true

# -----------------------------------------------------------------------------
# Additional Options
# -----------------------------------------------------------------------------
# Set to "Y" to accept the SQL Server End-User License Agreement
ACCEPT_EULA=Y

# Container restart policy (no, on-failure, always, unless-stopped)
RESTART_POLICY=unless-stopped

# -----------------------------------------------------------------------------
# Production Configuration Notes
# -----------------------------------------------------------------------------
# For production environments, you should:
#   1. Use significantly more complex passwords (20+ characters recommended)
#   2. Rotate passwords regularly
#   3. Store passwords in a secrets management system (e.g., Azure Key Vault)
#   4. Use separate .env files for different environments
#   5. Enable TLS/SSL and set TrustServerCertificate=false
#   6. Restrict network access using firewall rules
#   7. Enable SQL Server audit logging
#   8. Use Azure SQL Database or managed SQL Server for better security
# -----------------------------------------------------------------------------